#pragma once
// Defines names for use in layer keycodes and the keymap
enum layer_names {
    _BASE,
    _SYMBOLS,
    _DIGITS,
    _MOVEMENT,
    _WINDOWS,
    _SHORTCUTS,
    _EMPTY,
    _BLOCKED,
};

